# Code generated by servicegen. DO NOT EDIT.
# This overlay is the only project-level Compose configuration that mounts
# service source code. Use it through `make docker-up-dev`.
services:
  analyticsservice:
    image: analyticsservice-cpp-build
    build: !reset null
    working_dir: /workspace
    command:
      - /bin/sh
      - -c
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec gdbserver :2345
          /workspace/build/example_analytics_service
          --config /workspace/static_config.yaml
          --config_vars /workspace/config/config_vars.integration.yaml;
        else
          exec /workspace/build/example_analytics_service
          --config /workspace/static_config.yaml
          --config_vars /workspace/config/config_vars.integration.yaml;
        fi
    volumes:
      - ./analyticsservice:/workspace:ro
      - cpp-dev-cmake-build-analyticsservice:/workspace/build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  automationservice:
    image: automationservice-development:latest
    build: !reset null
    working_dir: /workspace/service
    command:
      - /bin/sh
      - -c
      - >-
        prepare-local-build /workspace/source /workspace/service
        && if [ "$${DEBUG:-0}" = "1" ]; then
          exec dlv debug ./cmd/service/main.go
          --headless --listen=:2345 --api-version=2 --accept-multiclient --continue --
          -config ./config/config.yaml -values ./config/overrides.yaml;
        else
          exec go run ./cmd/service/main.go
          -config ./config/config.yaml -values ./config/overrides.yaml;
        fi
    environment:
      GOPROXY: ${GOPROXY:-direct}
      GOSUMDB: ${GOSUMDB:-sum.golang.org}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - ./automationservice:/workspace/source:ro
      - go-dev-worktree-automationservice:/workspace/service
      - go-dev-mod-cache:/go/pkg/mod
      - go-dev-build-cache:/root/.cache/go-build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  inventoryservice:
    image: inventoryservice-cpp-build
    build: !reset null
    working_dir: /workspace
    command:
      - /bin/sh
      - -c
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec gdbserver :2345
          /workspace/build/example_inventory_service
          --config /workspace/static_config.yaml
          --config_vars /workspace/config/config_vars.integration.yaml;
        else
          exec /workspace/build/example_inventory_service
          --config /workspace/static_config.yaml
          --config_vars /workspace/config/config_vars.integration.yaml;
        fi
    volumes:
      - ./inventoryservice:/workspace:ro
      - cpp-dev-cmake-build-inventoryservice:/workspace/build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  orderservice:
    image: orderservice-cpp-build
    build: !reset null
    working_dir: /workspace
    command:
      - /bin/sh
      - -c
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec gdbserver :2345
          /workspace/build/example_order_service
          --config /workspace/static_config.yaml
          --config_vars /workspace/config/config_vars.integration.yaml;
        else
          exec /workspace/build/example_order_service
          --config /workspace/static_config.yaml
          --config_vars /workspace/config/config_vars.integration.yaml;
        fi
    volumes:
      - ./orderservice:/workspace:ro
      - cpp-dev-cmake-build-orderservice:/workspace/build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE

volumes:
  go-dev-mod-cache:
  go-dev-build-cache:
  rust-dev-cargo-registry:
  rust-dev-cargo-git:
  cpp-dev-cmake-build-analyticsservice:
    name: cppexample_analyticsservice_cpp-cmake-build
  go-dev-worktree-automationservice:
  cpp-dev-cmake-build-inventoryservice:
    name: cppexample_inventoryservice_cpp-cmake-build
  cpp-dev-cmake-build-orderservice:
    name: cppexample_orderservice_cpp-cmake-build
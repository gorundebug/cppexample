# model

Go module path: `github.com/gorundebug/cppexample/model_go`

## Make commands

```bash
make gen-proto           # [host] generate protobuf code
make fmt-proto           # [host] format .proto files
```
